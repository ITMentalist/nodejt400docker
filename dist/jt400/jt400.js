"use strict";
/*

const config = {
    host: '150.150.6.7',
    user: 'atl1arai',
    password: 'banco123'
}
const pool = require('node-jt400').pool(config);

pool
  .query('SELECT llave FROM arai.prueba WHERE llave=? ', [1])
  .then(( result:any) => {
    console.log('result');
    const llave = result[0].llave;
    console.log(llave);
  })
  .fail( (error:any) => {
    console.log('error');
    console.log(error);
  });

  */ 
